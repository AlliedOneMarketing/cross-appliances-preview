# Changelog

## 2026-07-26

- Navigable header: utility strip with tap-to-call, full nav, call button, staff switch
- Mobile drawer with all pages, both addresses, hours and a full-width call button
- Fixed horizontal scroll on phones (grid/flex `min-width:auto` overflow)
- Fixed the same overflow on the delivery date picker
- All tap targets raised to 44px+ on touch devices
- Inputs raised to 16px to stop iOS focus-zoom
- Filter bar no longer sticky on mobile
- Full-screen modals with safe-area insets
- Verified: zero horizontal scroll, zero undersized tap targets on iPhone SE / iPhone 15 / Pixel 8 / Galaxy S / iPad mini / iPad

## Earlier

- Seeded inventory with rendered product shots, spec sheets and test checklists
- Staff console behind a login
- Seeded a week of deliveries, service calls, payments and financing leads
- Locations page with storefront photos, locator maps and per-store stock
- Financing page with partner entry points and a payment estimator
- SEO footer with LocalBusiness schema for both locations
- `noindex` + `robots.txt` + `X-Robots-Tag` so the prototype can never be indexed
